#!/usr/bin/env python
# -*- coding: utf-8 -*-
import json

output = {}

# ============================================================
# Paper 0: 2512_01363v1
# ============================================================
output['2512_01363v1'] = {
    'technical': (
        "整体框架包含三个核心阶段：VLM高层场景分析与对抗性交互提议、社会感知奖励函数构建、以及基于进化策略引导的多智能体扩散联合轨迹生成。\n\n"
        "阶段一：VLM语义场景描述与对抗性提议生成。给定从真实驾驶数据集中提取的完整场景(S,M)，其中S包含所有车辆在时间T内的状态序列（位置、速度、航向角），M为视觉地图数据。"
        "VLM首先分析每辆车的状态向量s_t=[p_t, v_t, θ_t]，生成自然语言描述，捕捉相对空间位置、相对速度等动态特征并推断底层社会意图。"
        "在此基础上，VLM充当策略提议者，以场景描述和地图数据为条件，从所有车辆中选出具有最高社会交互潜力的车辆对(a_i, a_j)，生成针对这两辆车的对抗性自然语言指令(l_i, l_j)。"
        "这些指令规定高层意图（如"完成一次激进变道"），作为下游轨迹生成的关键条件信号，确保最终生成的场景既具高交互性又具多样性。\n\n"
        "阶段二：社会感知奖励函数。与传统将社会价值取向(SVO)表示为一维谱系的做法不同，本方法将社会动机沿两条独立轴分解。"
        "(1)内在奖励项R_intr：反映驾驶员的内在偏好，包含舒适性（车道保持、速度维持、航向一致性）和安全规范遵守。"
        "(2)外在奖励项R_extr：由LLM根据VLM生成的对抗性提议动态解析具体任务目标（如"成功完成变道"），并调用预定义奖励API实例化为具体奖励函数。"
        "综合奖励函数为R_i=cos(φ_svo,i)·R_intr,i+sin(φ_svo,i)·R_extr,i，其中φ_svo,i为社会取向角，控制智能体在内在价值与外在目标之间的倾向程度；β_i为内在奖励幅值。"
        "关键设计理念：SVO仅作用于他人的内在价值（安全、舒适），而非其不可观测的外在任务目标，从而更现实地建模驾驶员的社会偏好——驾驶员只能观察他人的内在状态，无法知晓其隐藏的行车目的。\n\n"
        "阶段三：进化策略引导的多智能体扩散联合生成。将标准单步逆向扩散过程x_{t-1}=μ_θ(x_t,t)+σ_t·ε扩展为多智能体联合轨迹分布建模。"
        "核心创新在于将基于奖励的进化搜索嵌入每个迭代去噪步骤中。具体流程：(1)以初始轨迹种群开始，每个搜索步骤对全体种群重新加噪以促进探索；"
        "(2)在每个去噪步t，评估整个噪声种群{x_t^(i)}，为每个样本预测对应的干净轨迹x̂_0^(i)并计算其奖励R(x̂_0^(i))；"
        "(3)构建奖励加权的精英分布p_e(x_t)∝exp(R(x̂_0)/τ)，其中温度参数τ采用退火调度——早期去噪步τ较小以鼓励探索（diversity），后期τ增大以转向利用（high-reward exploitation）；"
        "(4)从该分布采样精英种群作为下一步去噪的基础，逐步引导生成过程向高奖励解方向收敛。"
        "最终将单智能体框架扩展为多智能体联合建模：通过p(x^(1),...,x^(N)|l)显式捕捉智能体间依赖关系，使用社会感知奖励函数引导整个联合优化过程，确保集体行为既协调物理合理又符合预期的博弈论交互模式。"
    )
}

# ============================================================
# Paper 1: 2512_04021v1 - CompGS
# ============================================================
output['2512_04021v1'] = {
    'technical': (
        "CompGS是一个基于Transformer的紧凑型3DGS解码器，输入多视图图像{I_v}(v=1,...,V)，输出一组紧凑的3D Gaussians G={G_k}(k=1,...,N)。"
        "每个Gaussian参数化为G_k=(μ_k,α_k,Σ_k,c_k^sh)，其中μ_k∈R^3为中心位置，α_k∈[0,1]为不透明度，Σ_k=R_k·S_k·S_k^T·R_k^T为协方差矩阵，c_k^sh为带L阶的球谐系数编码视角相关颜色属性。\n\n"
        "多视图特征提取。对于输入图像I_v∈R^{H×W×3}，首先通过预训练视觉编码器E_vis（默认采用VGGT，已从大规模几何学习中习得丰富的多视图几何先验）提取特征图F_v∈R^{H_f×W_f×d}。"
        "与现有方法不同，CompGS不要求逐像素Gaussian预测，而是通过可学习查询令牌来实现更灵活的表示分配。\n\n"
        "可学习查询令牌与场景抽象。核心架构是一组可学习的查询令牌Q={q_k∈R^d}(k=1,...,N)，每个令牌负责发现并表示3D场景的特定区域。"
        "这些令牌作为抽象单元，从多视图特征中聚合相关信息形成连贯的3D Gaussians。"
        "与逐像素Gaussian估计方法（刚性映射每个像素到Gaussian）相比，查询令牌可灵活关注任意输入视图中的任意区域，学习将表征容量分配到最需要的位置。\n\n"
        "自注意力融合与双向信息流。将可学习查询令牌与图像特征拼接为统一序列[Q,F_1,...,F_V]，通过多层Transformer的全自注意力处理。"
        "这种设计实现了三种信息流动：(1)查询令牌→图像特征：聚合跨视图特定区域的视觉信息；"
        "(2)查询令牌↔查询令牌：交换信息以避免冗余并确保全面覆盖；"
        "(3)图像特征↔图像特征：跨视图特征交互增强几何一致性。\n\n"
        "Gaussian解码。经过Transformer处理后，提取精炼后的查询令牌q'_k，每个通过轻量MLP头解码为一个完整的Gaussian："
        "μ_k=MLP_μ(q'_k)，α_k=σ(MLP_α(q'_k))，Σ_k=MLP_Σ(q'_k)，c_k^sh=MLP_c(q'_k)。"
        "与其他需要真值标签的场景分解方法不同，采用3DGS作为表示使整个框架可仅通过新视角合成目标进行端到端学习。\n\n"
        "渲染与训练目标。在已知相机姿态的目标视点渲染预测的3D Gaussians：C=Σ_{k∈K}c_k·α'_k·Π_{j<k}(1-α'_j)，其中c_k为球谐解码的视角相关颜色，α'_k为2D投影后的加权不透明度。"
        "训练损失为L=λ_mse·||I_render-I_gt||^2+λ_lpips·LPIPS(I_render,I_gt)，λ_mse和λ_lpips为超参数平衡两项。\n\n"
        "渐进低通滤波策略。针对feed-forward训练中Gaussian位置预测不准确导致的稀疏梯度和模式坍塌问题，采用RAIN-GS的渐进低通滤波。"
        "对Gaussian G_k的2D投影协方差引入尺度因子s：Σ'_2D=J·W·Σ_k·W^T·J^T+s·I，其中J为投影变换的雅可比仿射近似，W为视角变换矩阵。"
        "训练初期s较大（如s=3.0），使Gaussian从扩大区域学习梯度信号；随训练推进逐步退火s→0，实现细粒度建模。该策略确保在位置预测欠优的早期训练阶段提供鲁棒梯度，同时随着网络学习准确位置后实现细粒度细节建模。\n\n"
        "涌现的多视图对应特性。尽管未对查询令牌如何划分场景提供任何监督，模型最终学会了仅从光度重建目标出发有效估计最优Gaussian集合。"
        "对自注意力权重的分析揭示了涌现特性：当选中某个Gaussian并可视化其对应查询令牌在各输入视图上的注意力图时，这些注意力在不同视点间聚焦于空间上一致的区域，有效发现了多视图对应关系而无需任何显式监督。"
        "这一涌现行为的驱动力在于：为了用有限数量的Gaussians准确重建新视角，模型必须学习将3D Gaussians定位到几何一致的区域。\n\n"
        "特征提升（Feature Lifting）。基于上述涌现特性，提出将任意2D特征提升为视角不变3D特征的简洁方法。"
        "传统方法面临两大挑战：(1)对应关系识别——需确定哪些3D Gaussian对某像素位置渲染有贡献，通常需要计算昂贵的反向映射操作；"
        "(2)多视图不一致——各视图独立编码导致同一3D区域的图像块产生不同特征表示。"
        "CompGS利用学习到的自注意力模式同时解决这两个问题：注意力权重直接提供了跨视图像素到Gaussian的对应关系（无需反向映射），同时注意力权重本身可作为聚合权重将多视图特征无歧义地融合为视角不变的3D特征。"
    )
}

# ============================================================
# Paper 2: 2512_07165v1 - MuSASplat
# ============================================================
output['2512_07165v1'] = {
    'technical': (
        "MuSASplat从未标定RGB图像{I_i}出发预测各向异性Gaussians用于新视角合成。仅三个轻量模块——Multi-Scale Adapter(MuSA)、Feature Fusion Aggregator(FFA)和Gaussian/Point预测头——可训练，大ViT骨干保持冻结。\n\n"
        "3DGS背景与问题定位。3DGS用各向异性3D Gaussians集合表示场景辐射场，每个Gaussian由均值位置μ、不透明度α、协方差Σ=R·S·S^T·R^T（S为对角缩放矩阵，R为旋转矩阵）和球谐系数c参数化。"
        "传统3DGS通过逐场景迭代优化拟合Gaussians，不适用于可泛化feed-forward模型。现有可泛化方法虽可直接预测像素对齐的3D Gaussians，但依赖已知相机参数。"
        "MuSASplat通过DUSt3R范式估计每视图pointmap并投影到规范空间，从无标定图像获得一致的3D Gaussian监督。\n\n"
        "DUSt3R基础。给定两张输入图像，DUSt3R预测稠密3D点云（pointmap）X_{i,j}，为图像i的像素j建立到相机i坐标系下3D点的映射。"
        "通过在同一共享坐标系中回归两个pointmap X_{1,1}和X_{2,1}，同时实现相机标定与3D重建。\n\n"
        "MuSA：多尺度空间适配器。LoRA因无法恢复空间先验而在3D任务中表现受限——ViT将每张图像处理为独立patch token序列，丢弃了patch内和patch间的2D结构信息，"
        "LoRA仅操作于1D token的线性变换而不恢复任何邻近性、局部结构或连续性概念。"
        "MuSA的设计直接针对此根本局限：将token序列重新解释为特征图，使用多核尺寸的深度可分离卷积恢复空间推理。具体步骤：\n"
        "(1)对每个输入图像I_i，冻结编码器产生token序列T_i∈R^{P×d}，通过线性投影降维Z_i=T_i·W_proj∈R^{H_p×W_p×d'}（降维比r），重构为空间网格；\n"
        "(2)并行应用核尺寸为3×3、5×5、7×7的三个深度可分离卷积，取平均值进行多尺度聚合Z'_i=Avg(Conv_{3×3}^D(Z_i), Conv_{5×5}^D(Z_i), Conv_{7×7}^D(Z_i))，捕捉细粒度和粗粒度空间依赖；\n"
        "(3)通过1×1点卷积和GELU激活后投影回原始维度d，加残差连接T'_i=T_i+Proj_out(σ(PConv(Z'_i)))。"
        "所有可学习参数零初始化，确保微调开始时模型行为等同于预训练骨干。"
        "实验还尝试了mini-grid分支来恢复patch内子结构——将每个token展开为k×k小特征图，应用深度卷积后平均回token大小——但该分支权重始终接近零且无任何可测量提升。"
        "此负面结果具有启发性：MuSA的主要收益并非来自恢复子patch细节，而是利用被ViT丢弃的跨patch空间关系。\n\n"
        "FFA：特征融合聚合器。替代Spann3R/CUT3R等方法需要O(N)次读写的迭代式外部记忆库。FFA一次性处理所有视图：将编码特征堆叠为F_stack∈R^{V×P×d}。"
        "质量估计器（轻量MLP）为每个token评估置信度s_i=σ(MLP_q(f_i))∈[0,1]；边界检测器从全局池化特征检测边界视图指示器b_v∈{0,1}，通过可调权重ω_b增强边界视图的贡献。"
        "最终注意力权重w_i=s_i·(1+ω_b·b_v(i))，低置信度token(s_i<0.5)被直接掩码。"
        "对每个查询视图，其余视图作为键/值对进行单次交叉注意力融合，再通过MLP残差连接F'_v=F_v+MLP(F_attn,v)。"
        "该聚合器仅调用一次，延迟与视图数V无关，峰值GPU内存减少约30%，训练加速约4.2倍。\n\n"
        "点云增强。当输入仅两视图且重叠小于30%时，Gaussian数量不足可能导致未覆盖区域出现空洞或过大Gaussian。"
        "解决方案：从同一冻结ViT骨干预测初始稀疏点云P_init和相对姿态，通过插值相机姿态光栅化合成额外视图I_syn，追加到输入中以增加Gaussian总数并填补空洞。\n\n"
        "训练损失。总目标L=L_RGB+λ_mv·L_mv_consistency。L_RGB遵循NoPoSplat的设计，为MSE和感知LPIPS损失的加权和。"
        "L_mv_consistency通过可微分Gaussian Splatting在多视图间强化渲染一致性，λ_mv为平衡系数。"
    )
}

# ============================================================
# Paper 3: 2512_07237v2 - UCPE
# ============================================================
output['2512_07237v2'] = {
    'technical': (
        "UCPE提出统一相机位置编码(Unified Camera Positional Encoding)，使视频生成扩散Transformer能够处理具有异构投影模型（针孔、鱼眼、折反射、全景等）的相机输入，并在生成时实现精确的相机姿态控制。\n\n"
        "射线映射统一表述。所有相机模型，无论其投影类型，均可统一表述为图像坐标到3D射线的映射。"
        "对每个像素(u,v)，射线映射函数r(u,v)=(o,d)产生射线原点o∈R^3和单位方向d∈R^3（相机坐标系）。"
        "映射由模型特定参数集φ=(f_x,f_y,ξ,...)参数化。对中心相机（针孔），所有射线共享原点o=0；对非中心相机（折反射、全景系统），原点因像素而异。"
        "以统一相机模型(UCM)为例，给定相机到世界姿态(R,t)，世界空间射线表示为d_w=R·d，o_w=R·o+t。此统一表述为跨异构投影模型的几何推理提供共同基础。\n\n"
        "现有相机编码方法分析。(a)原始参数编码：将(f_x,f_y,ξ,...)直接编码为数值特征向量，缺乏几何直观性。"
        "(b)Plücker编码：将每条射线表示为六维向量(d,m)，其中m=o×d为射线矩，提供紧凑且数值稳定的描述，但作为绝对编码对坐标帧选择敏感。"
        "(c)相对编码：CaPE将注意力修改为Softmax((W_Q x_i)^T(R_i^T R_j)(W_K x_j)/√d)，以相对相机旋转为条件；GTA进一步对值矩阵施加变换；PRoPE将旋转替换为包含内参的投影矩阵P_i，捕获完整视锥体几何，但仅适用于针孔模型。\n\n"
        "射线级编码（Ray Encoding）。现有相对编码的一个关键局限：假设同视图内所有图像token共享单一线性投影函数，忽略了由镜头畸变和广角效果导致的像素级投影偏差。"
        "UCPE将编码从相机级升级为射线级：对每个图像token x_i构建其局部射线到世界矩阵M_i。"
        "每个token关联一条视线，由世界坐标系下的原点o_w,i和方向d_i参数化。构建局部射线坐标系的正交基：\n"
        "(1)z轴沿射线方向：e_z=d_i；\n"
        "(2)x轴：e_x=normalize(e_down×d_i)，其中e_down为相机的下方向；\n"
        "(3)y轴：e_y=d_i×e_x。\n"
        "此正交基{e_x,e_y,e_z}与原点o_w,i共同构成射线到世界变换矩阵M_i=[e_x,e_y,e_z|o_w,i]∈R^{3×4}。"
        "每个token的特征因此与其特定的几何算符M_i关联，注意力机制可在射线空间中推理，而非在相机帧内共享相同的位置编码。\n\n"
        "方向编码（Orientation Encoding）。相对射线编码虽实现跨异构相机的几何一致性推理，但不提供绝对方向信息，导致首帧方向模糊。"
        "UCPE采用Lat-Up图（Latitude-Up map）注入全局方向上下文：(a)Latitude Map对每条世界空间射线计算仰角φ_i=arcsin(d_i·up_world)，正值对应仰视射线，捕获相机旋转通过外观线索（天地分离、物体对齐）的体现；"
        "(b)Up Map将每条世界空间射线绕局部x轴旋转微小角度δ，投影回图像平面，计算归一化像素位移Δp=|proj(R_x(δ)d_i)-proj(d_i)|作为指向上的方向指示。"
        "Lat-Up编码为每个token提供全局方向上下文，使生成过程可显式控制相机俯仰(pitch)和翻滚(roll)。\n\n"
        "混合注意力编码。UCPE将相对射线编码与RoPE融合，兼顾射线空间和图像空间推理。对每个token x_i，构造块对角算符：前d/2维编码世界到射线变换M_i^{-1}，后d/2维使用RoPE编码图像空间位置。"
        "注意力操作变为：A_{i,j}=Softmax((W_Q x_i)^T(R_i^{ray})(R_j^{ray})^T(W_K x_j)+RoPE(i,j)/√d_k)。"
        "空间注意力模块可选择性地接收Lat-Up图作为输入，经线性层投影后作为偏置项加入输入token。\n\n"
        "Transformer集成与预训练保留。采用Wan作为预训练扩散Transformer骨干。"
        "UCPE不直接替换现有3D RoPE位置编码，而是在自注意力计算中额外叠加UCPE编码，保持已有编码路径完整，从而保留大规模预训练的视觉先验不被破坏。"
        "这种增量式集成策略使得模型可同时从预训练的时序生成能力和新注入的相机几何推理中获益。"
    )
}

# Save intermediate
with open('C:/Users/XPENG_USER/Documents/docs/research/feedforward_recovery/output_part1.json', 'w', encoding='utf-8') as f:
    json.dump(output, f, ensure_ascii=False, indent=2)
print("Part 1 done:", list(output.keys()))
