import json

output = {
  "2403_04116v3": {
    "technical": "X-Gaussian的完整技术流程如图[Figure N]所示，包含三个核心模块：角度-姿态长方体均匀初始化（ACUI）、辐射高斯点云模型以及可微辐射光栅化（DRR）。\n\n**辐射高斯点云模型**\n\n自然光成像与X射线成像存在根本差异：自然光依赖物体表面反射，点颜色随观察方向变化（各向异性），因此原始3DGS使用球谐函数（SH）来建模这种视角相关的颜色。X射线成像则基于射线穿透物体时的衰减效应，衰减程度取决于物体各向同性的辐射密度属性，因此点的辐射强度是视角无关的。基于这一分析，X-Gaussian重新设计了辐射高斯点云模型。\n\n具体而言，每个高斯点云不再学习SH系数，而是学习一个特征向量f来表征其固有的辐射属性。然后通过辐射强度响应函数（RIRF）来预测该点在任意观察方向下的辐射强度：将特征向量f与一组常数权重w进行线性组合后，经Sigmoid函数激活并归一化得到辐射强度值。该公式不包含观察方向d的影响，符合X射线各向同性成像的本质。同时，RIRF免去了SH函数的复杂计算，使得前向和反向过程比原始3DGS更快。\n\n因此，辐射高斯点云集合表示为{mu, Sigma, alpha, f}，其中mu为中心位置，Sigma为协方差矩阵（由旋转矩阵R和缩放矩阵S组成，即Sigma=RSS^T R^T），alpha为不透明度，f为辐射特征向量。\n\n**可微辐射光栅化（DRR）**\n\nDRR将3D高斯点云渲染为2D投影图像，整体过程如图[Figure N(c)]所示。首先计算第i个高斯分布在3D点位置x处的概率值，该值由高斯分布公式给出。然后将3D高斯投影到2D探测器平面：先将中心位置mu从世界坐标系变换到相机坐标系，再投影到图像坐标系。同时，3D协方差矩阵Sigma也变换到相机坐标系下的Sigma'，变换涉及投影变换的仿射近似的雅可比矩阵J和观察变换矩阵W。2D协方差矩阵通过丢弃Sigma'的第三行和第三列获得。\n\n接下来，2D投影被划分为不重叠的瓦片（tile）。每个3D高斯根据其2D投影范围分配到相应瓦片中，如[Figure N(c)]左图所示。这些3D高斯按到2D探测器的距离排序。像素u处的强度通过对该像素在对应瓦片中重叠的排序点进行alpha混合得到：C(u)=Sigma_i T_i alpha_i I_i，其中T_i=Pi_{j=1}^{i-1}(1-alpha_j)，I_i为第i个高斯点的辐射强度，alpha_i经2D高斯分布在像素u处的值调制。\n\n训练目标为渲染图像与真实投影图像之间的L1损失和SSIM损失的加权和。通过最小化该损失，优化3D高斯的各项属性。自适应控制策略动态调整高斯点云的数量和分布。与原始3DGS的RGB光栅化相比，DRR在前后向过程中避免了与观察方向相关的复杂计算，因此训练成本更低、推理速度更快。\n\n**角度-姿态长方体均匀初始化（ACUI）**\n\n自然光成像中，原始3DGS使用SfM算法从多视角图像中检测和匹配特征来计算初始点云位置和相机参数。然而X射线图像具有灰度低对比度的特点，且不同物体层可能在投影的同一位置重叠，导致SfM的特征检测和匹配精度严重下降。此外，SfM运行耗时较长。\n\n针对圆形锥束X射线扫描场景（扫描仪发射锥形X射线束，以等角间隔采集投影），X-Gaussian设计了ACUI策略。如图[Figure N(a)]所示，ACUI直接利用X射线扫描仪的参数计算外参矩阵E和内参矩阵K：外参由源到物体的距离、方位角和仰角（设为零且保持不变）确定；内参由源到探测器的距离等参数确定。\n\n对于3D高斯中心位置的初始化，ACUI设置一个能完全包裹被扫描物体的长方体，尺寸为S_x * S_y * S_z（单位mm），该长方体的中心也是物体中心和世界坐标系原点。然后以网格尺寸V_x * V_y * V_z（体素）划分该长方体，以间隔d在网格内均匀采样点作为初始高斯中心位置。缩放矩阵S和旋转矩阵R的初始值根据网格大小和元素设定。ACUI避免了SfM的运行，使X-Gaussian享有更快的训练速度。"
  },
  "2403_11447v1": {
    "technical": "Motion-Aware 3DGS是一个针对动态场景重建的增强框架，核心思想是充分挖掘光流中的运动线索来改进现有的动态3DGS方法。该方法可与基于迭代和基于变形的两类动态3DGS范式结合，整体流程如[Figure N]所示。\n\n**3D高斯运动与像素级光流的对应关系**\n\n动态场景重建的核心问题是如何有效建模时域3D场景流。在3DGS框架下，场景流可以直观地映射为高斯中心点的运动。为了将这些运动与从图像序列中提取的2D光流对齐，需要建立稠密的跨维度运动对应关系。\n\n一个看似合理的方案是利用3DGS的可微渲染器将场景流投影到图像平面。但实践中发现，这种基于渲染的监督在应用于光流时表现不佳，有时甚至导致优化崩溃。原因有二：其一，与深度不同，光流定义并预测在2D图像平面，导致基于渲染的流监督存在显著差距；其二，高斯影响范围理论上无限远，使优化易受噪声和数值不稳定的影响。\n\n为解决这些问题，该方法提出了前景高斯搜索与重投影策略。当优化时间戳t的运动时，从上个时间戳t-1渲染过程中产生的深度图出发（在迭代范式中t-1场景已优化完毕，在变形范式中经过初始粗训练后也已较好建模，因此深度图具有中等精度）。通过深度值进行反投影找到像素的3D位置，将该位置周围的最近高斯作为运动优化的前景候选。为建立一一对应，选择对该位置贡献最大的高斯。但为了更加鲁棒和高效，采用软选择方式：在一次迭代中使用所有附近候选及其贡献。将所有候选的3D中心重投影到当前时间戳的图像平面，产生一组保留梯度的2D流用于后续运动优化。关键在于，一个像素的所有前景候选都由同一个精确的2D光流监督。\n\n**基于不确定性的流增强**\n\n获得每个像素的投影场景流后，将其与预训练光流预测器从相邻帧产生的光流先验对齐。然而该过程存在不确定性：一是光流预测器和相机标定固有误差（尤其在单目场景中相机抖动时）；二是像素级流并不总与高斯中心运动一致，考虑到大高斯的存在以及涉及缩放和旋转的复杂运动。\n\n为此，该方法提出基于KL散度的损失函数进行流对齐：最小化光流先验分布和预测分布之间的KL散度。该损失最终推导为先验流和预测流之间的加权差值，外加一个正则化项以避免平凡解。方差公式中使用归一化的高斯贡献和可学习的视角相关置信度来建模：贡献越小、不确定性越高的高斯会产生越大的方差，使该像素的流损失项自适应降低。\n\n**动态感知损失增强**\n\n除了直接的运动监督，还利用光流先验生成动态图/掩码，为优化过程中的其他正则化提供动态感知。以像素级颜色重建损失为例：计算2D光流图的归一化幅度得到动态图，然后在计算图像损失前用该图对预测值和真值进行加权。光流先验起到注意力图的作用，引导优化器更关注当前帧运动较大的区域。使用超参数平衡动态感知项和原始损失，以防光流预测器对微小运动产生无意义结果。\n\n此外，在迭代3DGS范式中存在物理损失，用于约束局部区域内高斯的运动相似性。原有方法使用3D前景掩码来区分动态和静态高斯，但该掩码依赖2D分割先验，与真正的动态区域往往存在差异。本方法基于动态图设计3D动态掩码：利用前述对应关系获取像素级运动幅度的稠密对应，结合当前帧所有视角的流信息并设置阈值，为所有高斯赋予动态标签。这样无需分割的额外成本就能实现更准确的动态感知局部运动约束。\n\n**瞬时感知变形辅助模块**\n\n相比迭代范式，变形范式试图联合建模所有时间戳的场景，是一个更困难的优化问题。前述流增强方法虽仍可应用，但性能增益有限。原因在于：迭代范式中t-1时刻已优化好的场景可视为固定锚点，光流先验引导高斯向当前时刻t的明确目标移动；而变形范式中t和t-1时刻的场景都不固定，缺乏锚点时仅优化两者相对运动存在歧义。\n\n因此需要辅助设计来增强变形。基于基线方法的HexPlane结构，提出显式向时变体素特征中注入瞬时信息。使用速度场解码每个高斯的位置和时间相关特征为3D瞬时速度v。通过稠密对应搜索将v与光流（除以时间间隔Delta t）对齐，提供额外的正则化信号，从而在变形范式中增强了优化的稳定性和最终渲染质量。"
  },
  "2404_03202v5": {
    "technical": "OmniGS是一个利用全向（等距矩形）图像进行快速辐射场重建的全向高斯溅射系统。其关键在于从球面相机的理论分析出发，推导出3D高斯溅射中球面相机模型的导数，并据此实现了GPU加速的全向光栅化器，直接在等距矩形屏幕空间上溅射3D高斯，无需立方体贴图校正或切平面近似。\n\n**系统整体流程**\n\n重建过程从一组经SfM标定的等距矩形图像{I_i}开始，每张图像具有位姿T_i。从彩色稀疏SfM点云中获得初始3D高斯，其旋转、缩放和不透明度初始化为单位值。然后开始一系列优化迭代：每次迭代从随机打乱的图像集中选择一个视角，对该视角渲染得到I_render，计算光度损失及对应的反向梯度。\n\n**基于梯度的稠密化控制**\n\nOmniGS采用与3DGS类似的基于梯度的稠密化控制策略，但关键区别在于不使用透视梯度来判断是否稠密化，而是使用高斯在全向屏幕空间位置上的梯度。这些梯度在渲染计算过程中被导出和记录。具体而言，对于梯度足够大的高斯：如果其尺度过大，则将其分裂；如果尺度过小，则将其克隆，从而增强表示细节的能力。与此同时，对尺度或屏幕空间半径过大的高斯进行剪枝以提升细节表现。所有高斯的不透明度也参与稠密化控制：不透明度极小的高斯被剪枝；过大的不透明度被重置以鼓励更多稠密化。该稠密化控制过程周期性执行，直到达到预设的迭代次数。\n\n**全向光栅化核心**\n\nOmniGS的核心贡献是全向光栅化器的数学推导。与透视相机将3D高斯投影到平面屏幕空间不同，全向相机直接将3D高斯溅射到等距矩形（经纬图）屏幕空间。这需要对投影变换和协方差传递进行重新推导：3D高斯中心通过球面投影映射到等距矩形坐标而非透视坐标，协方差矩阵的传递也需考虑球面投影的雅可比矩阵。由于直接在球面域上进行溅射，避免了传统方法中先做立方体贴图校正再在各面上分别进行透视投影的繁琐步骤，也避免了切平面近似带来的精度损失。OmniGS实现了这一全向光栅化器的CUDA加速，在训练和推理中均保持了与原始3DGS相当的高效率。"
  },
  "2405_15125v4": {
    "technical": "HDR-GS的整体框架如图[Figure N]所示，包含三个核心组件：首先使用SfM算法重新标定相机参数并初始化高斯点云（图(a)）；然后提出双动态范围（DDR）高斯点云模型联合拟合HDR和LDR颜色（图(b)）；最后通过两个并行的可微光栅化（PDR）过程分别渲染HDR和LDR视图（图(c)）。\n\n**双动态范围（DDR）高斯点云模型**\n\nDDR高斯点云模型对每个3D高斯同时建模HDR颜色和LDR颜色。场景由N个DDR高斯点云表示，每个高斯i包含：中心位置mu、协方差Sigma（由旋转矩阵R和缩放矩阵S构成）、不透明度alpha、LDR颜色c^LDR、HDR颜色c^HDR以及曝光时间e。此外还包含三个全局共享的MLP参数theta_tm用于色调映射。\n\n色调映射（tone-mapping）操作建模了相机响应函数（CRF），将HDR颜色非线性映射为LDR颜色：c^LDR = CRF(e . c^HDR)。HDR-GS采用MLP来学习这一映射过程。为避免直接建模乘法形式c^LDR = MLP(e . c^HDR)带来的梯度消失问题（乘法可能导致数值溢出或下溢，且使MLP输入信号非线性不连续），采用Debevec和Malik的传统非参数CRF标定方法思路：将线性域变换到对数域以增强训练稳定性。具体操作是取反函数并变换：log(c^HDR) = log(CRF^{-1}(c^LDR)) - log(e)。然后使用三个独立MLP分别在RGB通道上建模该函数，因为RGB各通道的CRF不同。定义色调映射器为T(log(c^HDR) + log(e))。\n\nHDR颜色的对数log(c^HDR)由球谐函数（SH）建模，使用一组系数k={k_l | 0 <= l <= L}，每个k_l包含对应RGB三个分量的三组系数。在观察方向d下的HDR颜色为c^HDR = exp(SH(k, d))。代入色调映射公式得c^LDR = T(SH(k, d) + log(e) + b)，其中b为常数偏置，用于帮助调整SH函数更好地拟合数据。\n\n色调映射器T的架构如图[Figure N(b)]所示：RGB各通道的对数值分别经过一个独立MLP，包含一个全连接层（FC）、ReLU激活、一个FC层和Sigmoid激活，最终输出LDR颜色。\n\n**并行可微光栅化（PDR）**\n\n计算得到的HDR颜色和LDR颜色分别输入两个并行的可微光栅化过程，联合渲染HDR和LDR视图。两个光栅化过程分别定义为R_HDR和R_LDR，输出渲染的HDR图像和指定曝光时间e下的LDR图像。\n\n具体流程：首先计算第i个3D高斯分布在点位置x处的概率值；然后将3D高斯投影到2D成像平面——中心位置mu从世界坐标系变换到相机坐标系再投影到图像坐标系，同时3D协方差Sigma变换到相机坐标系下的Sigma'（使用投影变换仿射近似的雅可比矩阵J和观察变换矩阵W的前三行三列），2D协方差通过丢弃Sigma'的第三行和第三列得到。\n\n2D投影被划分为不重叠的瓦片，3D高斯分配到其2D投影覆盖的瓦片中，在每个瓦片内按视图空间深度排序。像素u处的RGB值通过混合该像素所在瓦片中的排序点得到，HDR渲染值和LDR渲染值分别使用对应的颜色属性。\n\n**数据基础重建与训练**\n\n原始多视角HDR数据集的相机位姿使用归一化设备坐标（NDC）系统表示，不适用于3DGS：首先，NDC描述2D屏幕上透视投影后的位置，而3D高斯是显式3D表示，需要在3D空间中进行变换和投影；其次，NDC将坐标缩放到[-1,1]或[0,1]范围，体素分辨率受限，难以捕获场景细节。此外原始数据集不提供SfM点云用于初始化。\n\n为解决这些问题，HDR-GS使用SfM算法重新标定相机参数并计算初始高斯位置。SfM的映射函数为E, K = SfM({I_i^{LDR}(e_i)})，其中取相同曝光时间下的HDR视图作为SfM输入，因为SfM基于多视图特征检测和匹配，曝光条件变化会降低精度。计算得到的外参E和内参K用于初始化高斯点云的外参和内参属性，其他可学习参数随机初始化。\n\n训练时使用L1损失和D-SSIM损失的加权和。在合成场景上，额外施加HDR图像的mu-law LDR域约束：对渲染的HDR图像进行压缩量参数nu的mu-law压缩，再进行min-max归一化，与真值HDR图像同样处理后的结果计算L2损失。总训练损失为L_img与lambda_mu * L_mu的加权和（真实场景中不提供真值HDR图像，因此不使用L_mu）。"
  },
  "2406_03697v1": {
    "technical": "Superpoint Gaussian Splatting (SP-GS) 将3D高斯聚合为超点（superpoint），通过仅对超点施加时变变形来高效建模动态场景。核心思想是：每个3D高斯不应是完全独立的实体，相邻的3D高斯由于刚体运动的特性，在所有时间戳上可能具有相似的平移和旋转变换，因此可将其聚类为超点，避免为每个高斯单独计算变形。\n\n**超点与变形网络**\n\n将M个3D高斯划分为K个超点（K个不相交集合）。每个超点包含若干3D高斯，每个3D高斯仅属于一个超点。遵循尽可能刚体（As-Rigid-As-Possible）原则，同一超点内的3D高斯应具有相似的变形，这种变形可以通过相对平移Delta mu和相对旋转Delta R来表示。因此，第j个3D高斯在时刻t的中心位置和旋转矩阵为：mu_j(t) = R_k(t)(mu_j - mu_k) + mu_k + Delta mu_k(t), R_j(t) = Delta R_k(t) . R_j，其中mu_k为超点k在规范空间中的中心位置，R_j为高斯的规范旋转矩阵。\n\n为预测第k个超点在时刻t的相对平移和旋转，直接使用变形神经网络F_theta，输入为时间戳t和超点的规范位置gamma(mu_k)（经位置编码），输出超点相对于规范空间的变换：{Delta mu_k(t), Delta R_k(t)} = F_theta(gamma(mu_k), gamma(t))。位置编码gamma(.)使用标准的正弦余弦编码。推理时，对训练集中未出现的新时间戳，可通过线性插值计算变形：在两最近时间戳的变形之间按时间比例插值。\n\n**可学习关联矩阵与属性重建损失**\n\n超点的关键在于具有相似属性的点应聚合到同一组。通过可学习关联矩阵W in R^{M*K}建立3D高斯与超点之间的连接，每个高斯只考虑与之最近的I个超点。关联概率通过softmax在最近超点集上计算：w_{j,k} = softmax(W_{j,k})。\n\n利用关联概率，可从高斯属性重建超点属性：例如超点k的位置mu_k = Sigma_{j in N_k} w_{j,k} mu_j / Sigma_{j in N_k} w_{j,k}，其中N_k为与超点k关联的高斯集合。注意相对旋转采用李代数so(3)表示，支持线性旋转插值。同样，高斯的属性也可通过邻近超点重建。\n\n属性重建损失确保高斯原始属性与重建属性之间的一致性：L_recon = MSE(P_j, P_tilde_j)，其中P_j包括位置mu_j、相对平移Delta mu_j和相对旋转so(3)(Delta R_j)。同一超点内高斯属性越相似，该损失越小，从而充分利用了尽可能刚体的特性。每个高斯的对应超点即关联概率最高的超点。\n\n**训练与推理细节**\n\n变形网络F_theta为8层MLP，隐层维度256。总训练迭代40k次，前3k次迭代作为热身阶段不训练变形网络，使3D高斯达到相对稳定的位置和形状。规范空间中的3D高斯在热身训练后初始化。超点的初始化使用最远点采样算法从高斯中采样，超点的规范位置等于采样高斯中心。关联矩阵W中，若第k个超点由第j个高斯初始化，则W_{j,k}初始化为0.9，否则初始化为0.1。每次迭代前用当前关联矩阵计算超点的规范位置。\n\n使用Adam优化器，3D高斯的训练策略与3DGS相同。关联矩阵的可学习参数学习率从1e-3指数衰减到1e-5。总损失函数为L = L_img + lambda_recon L_recon，其中L_img为L1与D-SSIM的组合损失（与3DGS相同）。\n\n**可选的非刚体变形网络**\n\n考虑到动态场景中可能存在的非刚体变形，额外设计可选的非刚体变形网络N_phi，为每个高斯在时刻t学习非刚体变形{Delta mu'_j(t), Delta R'_j(t)}。最终高斯的中心位置和旋转矩阵为刚体变形与非刚体变形的组合：先施加非刚体变形到规范空间，再施加刚体变形。包含非刚体网络版本（SP-GS+NG）使用纯刚体版本的预训练模型初始化，额外训练20k迭代。N_phi为3层MLP，隐层维度64。"
  },
  "2406_06521v2": {
    "technical": "PGSR（Planar-based Gaussian Splatting Reconstruction）从多视角RGB图像实现高效高保真的几何重建，同时保持与3DGS相当的渲染质量。核心思路是将3D高斯压缩为2D平面表示，并基于该平面表示推导无偏深度渲染方法，随后引入单视图几何、多视图光度及几何一致性损失，辅以相机曝光补偿模型。\n\n**3D高斯展平与平面表示**\n\n3D高斯的协方差矩阵Sigma = RSS^T R^T表达了椭球形状，其中R为正交归一基（椭球三个轴的方向），S为各轴方向的尺度因子。通过压缩最小尺度因子s_min方向，可将高斯椭球展平为最接近其原始形状的平面。具体做法是直接最小化每个高斯的最小尺度因子：L_s = Sigma_i ||s_min,i||_1。展平后，最小尺度因子的方向对应于高斯的法向。由于最短轴有正负两个方向，通过观察方向来确定法向方向——即观察方向与法向的夹角应大于90度（确保法向指向相机侧）。最终当前视角下的法向图通过alpha混合渲染：N(u) = Sigma_i T_i alpha_i R_c^T n_i，其中R_c为相机到世界的旋转，n_i为高斯i的法向。\n\n**无偏深度渲染**\n\n平面到相机中心的距离为d_i = n_i^T (c - mu_i)，其中c为相机在世界坐标系中的位置，mu_i为高斯i的中心。当前视角下的距离图同样通过alpha混合渲染：D(u) = Sigma_i T_i alpha_i d_i。\n\n获得平面的距离和法向后，通过射线与平面求交来确定深度图：depth(u) = D(u) / (n(u)^T K^{-1} u_tilde)，其中u为图像平面上的2D位置，u_tilde为齐次坐标，K为相机内参。如[Figure N]所示，这种深度渲染方法相比以往方法有两个主要优势。第一，深度形状与展平高斯形状一致，能真实反映实际表面。以往方法直接基于高斯深度的alpha混合进行深度渲染，导致深度曲线化，与平面高斯形状不一致，产生几何冲突。PGSR先渲染平面的法向图和距离图，再转换为深度图，深度位于高斯平面上。当3D高斯平面贴合实际表面时，渲染深度与实际表面完全一致。第二，由于每条射线的累积权重可能小于1，以往渲染方法受权重累积影响，深度可能偏向相机侧且整体被低估。PGSR通过将渲染原点距离除以法向得到深度，有效消除了权重累积系数的影响。\n\n**单视图几何正则化**\n\n原始3DGS仅依赖图像重建损失容易陷入局部过拟合优化，导致高斯形状与实际表面不一致。PGSR引入几何约束确保3D高斯尽可能贴合实际表面。\n\n局部平面假设：假设一个像素及其邻近像素可近似为一个平面。渲染深度图后，使用固定模板采样四个邻近点。基于已知深度计算平面的法向，对整个图像重复该过程，从渲染深度图生成法向。然后最小化该法向图与渲染法向图之间的差异，确保局部深度与法向的几何一致性。\n\n图像边缘感知单视图损失：邻近像素不一定完全满足局部平面假设，尤其在边缘区域。使用图像边缘来近似几何边缘。对像素点u，从上、下、左、右四个方向采样邻近像素。将四个采样深度点投影到相机坐标系中的3D点，计算该像素的局部平面法向。最终的单视图法向损失为：L_sv = Sigma_{u in U} (1 - g(u)) ||n_tilde(u) - n_hat(u)||，其中g(u)为归一化到[0,1]的图像梯度，n_hat(u)为渲染法向。在边缘区域（g(u)接近1）损失权重降低，避免在几何不连续处强制平面假设。\n\n**多视图几何一致性**\n\n单视图几何正则化可维护深度与法向的局部一致性，但由于高斯点云优化的非规则离散特性，多视角间的几何结构并不完全一致。因此引入多视图几何正则化确保全局几何一致性。\n\n对于参考帧中的特定像素，其法向为n_ref，平面距离为d_ref。参考帧的像素可通过单应矩阵H映射到邻近帧像素：H = K_n (R - t n_ref^T / d_ref) K_ref^{-1}，其中R和t为参考帧到邻近帧的相对旋转和平移。类似地，邻近帧像素也可通过相应的单应矩阵反向映射。像素在参考帧和邻近帧之间进行前向和反向投影，建立多视图的几何对应。多视图一致性损失约束对应点之间的法向和平面距离一致。这种一致性先验约束有助于收敛到正确的几何解，增强几何平滑度。\n\n**相机曝光补偿模型**\n\n对于光照变化较大的场景，PGSR引入相机曝光补偿模型。该模型为每张图像学习一个曝光补偿仿射变换（包含缩放因子a和偏置b），对渲染图像进行曝光调整后再与真值比较，进一步增强重建精度。"
  },
  "2410_08017v3": {
    "technical": "FCGS（Fast Compression of 3D Gaussian Splatting）提出了一种免优化的3DGS压缩方案，通过单次前馈传播即可完成压缩，将压缩时间从数分钟缩短至数秒。其核心设计遵循两条基本原则：值基原则（value-based）和结构基原则（structure-based），在不改变3DGS结构完整性的前提下，保持渲染保真度。\n\n**免优化压缩管线**\n\n以往的3DGS压缩方法（如HAC、ContextGS）采用逐场景优化（per-scene optimization）的压缩管线：对每个场景需要额外的微调训练来弥补压缩带来的信息损失，虽然能获得较好的率失真（RD）性能，但压缩速度极慢。FCGS改以单次前馈传播实现压缩，无需任何微调，对3DGS的来源（无论是通过优化还是前馈模型获得）透明适用。这种设计的关键在于：不同于以往方法降低参数精度或改变3DGS结构（从而需要重新微调），FCGS同时保持参数值和结构完整性，使免优化设计成为可能。\n\n**多路径熵模块（MEM）**\n\nMEM在值基原则下工作，为每个高斯属性的不同分量推断掩码，决定哪些分量进入哪条熵约束路径。具体而言，高斯的不同属性（位置、缩放、旋转、不透明度、颜色）具有不同的重要性和统计特性，如果使用单一熵模型压缩所有属性，将难以在模型大小和重建质量之间取得最优平衡。MEM通过多条并行的熵约束路径，为不同属性或不同精度需求分配不同的压缩策略：重要属性走低压缩率路径以保持精度，次要属性走高压缩率路径以节省存储。MEM网络学习预测每个高斯属性分量对应的路径选择掩码。\n\n**高斯间与高斯内上下文模型**\n\n为了进一步消除非结构化高斯点云中的冗余，FCGS设计了高斯间（inter-Gaussian）和高斯内（intra-Gaussian）上下文模型。高斯内上下文模型利用单个高斯内部各属性之间的相关性进行熵编码：例如颜色属性通常与位置属性存在空间相关性，缩放与旋转影响高斯的覆盖范围从而关联颜色。高斯间上下文模型则利用邻近高斯之间的空间相关性：相邻高斯通常共享相似的几何和外观属性（如同一表面上的高斯具有相似的法向和颜色）。通过这两个层次的上下文建模，FCGS有效捕捉并消除了高斯点云内部的冗余信息，在不牺牲重建质量的情况下实现了超过20倍的压缩比，超越了大多数基于逐场景优化的SOTA方法。"
  },
  "2503_02279v1": {
    "technical": "本文使用DreamerV3算法训练交通信号控制（TSC）模型，探索世界模型在TSC策略学习中的优势。核心思路是利用DreamerV3从过去经验中总结环境的通用动力学知识，通过想象训练（imagination training）预测潜在行动的未来结果，从而大幅减少与真实环境的交互需求。\n\n**环境设计**\n\n在强化学习环境设计中，状态函数基于队列长度定义，用于捕捉交叉口各进口道的拥堵程度。奖励函数同样基于队列长度设计，引导策略朝减少队列长度（即缓解拥堵）的方向优化。动作空间设计为高效管理队列长度的控制决策，对应信号灯相位的选择与切换。\n\n**DreamerV3算法核心机制**\n\nDreamerV3是一个基于世界模型的强化学习算法，由三个核心组件构成：世界模型（World Model）学习环境的紧凑潜在表示和转移动力学；Actor（策略网络）在世界模型的潜在空间中通过想象训练学习最优动作策略；Critic（价值网络）估计状态价值以辅助Actor训练。世界模型接收环境的状态和动作输入，预测下一状态的潜在表示以及奖励。Actor和Critic完全在世界模型的潜在空间中进行训练，无需与环境交互。这种设计的优势在于：一旦世界模型学到了足够准确的动力学，后续的策略优化可以在潜在空间中无限进行想象推演，从而显著提高数据效率。\n\n**超参数调优与泛化分析**\n\n在SUMO仿真平台上，针对不同OD矩阵场景对DreamerV3的两个关键超参数——训练比率（training ratio，即想象训练步数与实际环境交互步数之比）和模型规模（model size）——进行调优和分析。研究发现：选择较小的模型规模并初步尝试几个中等训练比率，可以显著减少超参数调优的时间成本。此外，该方法具有一般适用性，同一组超参数可以解决两个不同TSC任务场景。关于DreamerV3声称的数据效率：由于训练早期回合奖励曲线波动较大，仅能确认较大模型规模表现出适度数据效率，未发现增大训练比率能加速收敛的证据。"
  },
  "2503_07152v1": {
    "technical": "该方法使用场景图（scene graph）作为可控条件生成3D室外场景，整体流程包括三个核心阶段：首先通过图神经网络将场景图转换为稠密BEV嵌入图（BEM），然后通过2D离散扩散模型从BEM生成2D语义地图，最后通过3D离散扩散模型从2D地图生成完整的3D场景。\n\n**场景图形式化定义**\n\n场景图由节点和边组成G=(V,E)。节点分为两类：实例节点V_ins表示可数物体（如车辆、行人），每个实例节点关联特征向量f_i=[a_i, p_i]，其中a_i为节点属性（语义标签等），p_i为BEV地图中的2D中心坐标。场景道路节点V_road为单一节点，定义道路结构和其他全局背景信息。边也分为两类：物理邻近边——任意两个实例节点间欧氏距离小于阈值tau时建立边；道路连通边——每个实例节点与道路节点之间建立边。实践中使用简化的场景图以方便用户交互：每个实例节点仅包含语义标签和近似的2D位置（表示为网格索引），道路节点以道路类型表示。\n\n**图神经网络与BEV嵌入图**\n\n场景图神经网络（如图[Figure N]所示）使用图注意力网络（GAT）生成捕获局部结构和全局上下文信息的节点嵌入。给定图G和邻接矩阵A，通过两层GAT计算节点嵌入。为融入全局上下文，将每个节点的嵌入与全局池化嵌入拼接：h^CANE_i = [h_i || MLP(Pool({h_j}))]。该输出称为上下文感知节点嵌入（CANE）。\n\nGNN的训练包含辅助任务和下游任务。辅助任务包括：使用图自动编码器（GAE）的边重建损失（BCE损失，重建邻接矩阵A的全局边结构）和节点分类损失（将每个节点分类回原始类别），确保网络学习结构关系和节点特征。下游任务中，CANE嵌入通过分配模块转换为BEV嵌入图（BEM，如图[Figure N]所示）：BEM = Sigma_i b_i * h^CANE_i，其中b_i为二值位置指示图。推理时位置通过MLP定位头（使用Gumbel softmax）采样，训练时使用真值位置。分配模块有效将不规则稀疏图表示转化为稠密2D图，便于后续2D扩散过程。\n\n**2D地图离散扩散**\n\n给定场景图可能存在多种符合其结构的2D地图。使用离散扩散模型将稀疏场景图嵌入转化为稠密2D地图表示M_2D in {0,...,C-1}^{H*W}，其中C为语义类别数。前向过程在T步内使用转移矩阵Q逐渐破坏2D地图，通过累积矩阵Q_bar_t可直接从原始地图采样加噪版本。反向过程中，模型p_theta学习从噪声图M_t预测去噪图M_{t-1}，以BEM为条件进行引导。损失函数为KL散度加辅助重建项。推理时从随机噪声出发，用学习的反向扩散生成稠密2D地图。GNN和2D地图扩散模型联合训练。\n\n**3D场景离散扩散**\n\n将生成的2D地图转换为稠密3D场景（如图[Figure N]所示），同样使用离散扩散过程。3D场景定义为M_3D in {0,...,C-1}^{H*W*D}。3D扩散遵循与2D情况相同的前向和反向步骤，但在3D场景网格上操作。可学习模型q_phi预测每步去噪状态，以输入2D地图M_2D为上采样条件。推理时从噪声3D状态开始，应用反向扩散过程生成与2D地图空间布局对齐的完整3D场景。\n\n**交互式控制系统**\n\n开发了交互式控制系统（如图[Figure N]所示），核心是一个图形界面，用户可通过节点添加、删除和位置调整等直观操作精确构建和修改场景图，实现细粒度控制。用户还可提供文本提示，由大语言模型处理生成对应的场景图，再输入到方法中生成最终3D场景。"
  },
  "2503_20523v1": {
    "technical": "GAIA-2是一个环视视频生成式世界模型，支持结构化条件控制、多相机一致性和高时空分辨率。架构（如图[Figure N]所示）由两个核心组件构成：视频分词器（Video Tokenizer）和潜在世界模型（Latent World Model）。\n\n**视频分词器**\n\n视频分词器将原始高分辨率视频压缩为紧凑连续的潜在空间，保留语义和时间结构。采用时空分解的Transformer架构，编码器和解码器不对称（分别为85M和200M参数）。编码器提取时空下采样的潜在表示，各时间帧独立处理；解码器利用时间上下文从潜在表示重建完整视频帧。\n\n给定输入视频V in R^{T*H*W*3}，编码器计算潜在token Z in R^{T'*H'*W'*D}。编码器空间下采样因子f_s=16，时间下采样因子f_t=4，潜在维度D=64，总压缩比为16*16*4*3/64=48倍。相比常见的8倍空间压缩并使用16通道的方案，GAIA-2采用更高的空间压缩率配合更大的通道数（64而非16）。这种参数化策略的优势在于：(1)更短的潜在序列实现更快推理和更好内存效率；(2)模型展示出更好的视频内容和时序动态捕获能力。\n\n与基于离散潜在变量的前代GAIA-1不同，GAIA-2采用连续潜在空间，改善了时间平滑性和重建保真度。编码器和解码器的关键区别在于：编码器独立地将连续帧映射到单个时间潜在，而解码器联合解码时间潜在以实现时间一致性。推理时使用滑动窗口解码视频帧（如图[Figure N]中Training和Inference所示）。\n\n视频分词器的训练使用像素重建损失和潜在空间损失的组合。为进一步提升视觉质量，在保持编码器冻结的情况下使用3D卷积鉴别器的GAN损失对解码器进行微调。鉴别器为一系列残差3D卷积块，基通道64，时空步长2，使用3D模糊池化、通道倍增因子[2,4,8,8]、3D实例归一化和LeakyReLU激活，配合谱归一化和softplus激活的原始GAN损失。\n\n**潜在世界模型**\n\n潜在世界模型以过去潜在状态、动作和丰富的条件输入为基础，预测未来潜在状态。采用时空分解Transformer架构，参数规模8.4B，使用流匹配（Flow Matching）进行训练以兼顾稳定性和样本效率。\n\n设输入潜在Z in R^{T*C*H'*W'*D}，其中T为时间窗口，C为相机数量。每个时间步提供动作向量a_t和条件向量cond_t。世界模型的隐藏维度为d。每个动作嵌入到R^d，条件向量嵌入到R^{N_cond*d}。流匹配时间t_flow通过正弦编码映射到R^d。\n\n流匹配时间和动作通过自适应层归一化（adaLN）注入每个Transformer块，其他条件变量则通过交叉注意力引入。将动作通过adaLN而非交叉注意力引入的原因是：动作影响每个空间token，adaLN提供了显式的信息门控通道，比依赖可学习注意力机制更精确。\n\n位置编码方面独立编码三部分：(1)空间token位置使用正弦嵌入；(2)相机时间戳使用正弦嵌入后经小型MLP处理；(3)相机几何信息（畸变、内参、外参）通过可学习线性层处理。所有位置编码在每个Transformer块开头与输入潜在相加。\n\n世界模型包含L个时空分解Transformer块，隐藏维度d，多头注意力。每个块包含空间注意力（跨空间和相机）、时间注意力、交叉注意力和带自适应层归一化的MLP层。为提高训练稳定性，在每个注意力层前使用查询-键归一化（QK-Norm）。\n\n训练时随机采样上下文帧数C（其中C=0对应从零生成），并按预定义分布采样流匹配时间。上下文潜在保持不变，未来潜在与随机高斯噪声线性插值：Z_t = (1-t)Z_future + t * epsilon。速度目标向量v = Z_future - epsilon。世界模型预测目标速度v_theta，以上下文潜在、动作和条件为输入，训练使用预测速度与目标速度之间的L2损失。\n\n**结构化条件接口**\n\nGAIA-2支持丰富的结构化条件输入实现细粒度生成控制，包括：自车动作、动态智能体属性（如3D边界框）、场景级元数据、相机配置、时间戳嵌入以及外部潜在表示（如CLIP或专有场景嵌入）。\n\n相机编码方面，分别计算内参（从内参矩阵提取焦距和主点坐标，归一化后投影到共享潜在空间）、外参和畸变系数的嵌入，然后求和形成统一的相机编码。这种配置使模型能有效融入真实世界的相机变化。条件机制通过自适应层归一化（动作）、加性模块（相机几何和时间戳）和交叉注意力（所有其他变量）的组合集成到世界模型中。"
  },
  "2505_02175v1": {
    "technical": "SparSplat提出了首个基于可泛化2D Gaussian Splatting的前馈方法，从稀疏视图图像中通过单次前向传播同时实现3D重建和新视图合成。核心思路是学习一个MVS-based的可泛化模型，将输入图像映射到任意目标视图的2DGS平面表面元素参数图。\n\n**2D高斯溅射（2DGS）基础**\n\n3DGS将场景表示为3D各向异性高斯集合，每个高斯由中心mu、缩放s、旋转q、不透明度alpha和SH编码颜色定义。协方差Sigma=RSS^T R^T，可通过世界到相机变换矩阵W和局部仿射变换J投影到屏幕空间。渲染时将预排序的高斯投影到图像平面并通过alpha合成混合。但3D基元在不同视角下可能产生歧义——同一个2D足迹可能对应多种3D基元配置，且3D高斯投影到图像平面所用的透视仿射近似随着点远离基元中心变得不准确。\n\n2DGS将基元表示为平面2D高斯，改善了多视图深度一致性。屏幕空间中每个高斯表示为G(u; P)，其中P为中心位置，u为屏幕空间像素坐标。射线-平面交点的UV坐标可表示为屏幕空间像素坐标的变换。射线与面片的交点具有闭式解，涉及单应矩阵H：H与面片在世界空间中的切向量t_u、t_v（由可学习的旋转q导出）、缩放因子s_u、s_v以及面片中心mu相关。之后如3DGS一样使用可学习的不透明度进行alpha混合渲染。该方法通过精确建模溅射渲染中的透视几何，获得了比3DGS更好的多视图深度一致性和3D重建质量。\n\n**前馈2DGS参数回归管线**\n\n给定输入图像{I_i}及其相机位姿（包含3D旋转和平移），模型预测目标视图v的像素对齐2DGS参数图：Phi_v = F({I_i}, {P_i}, P_v)。\n\n预测管线沿用pixelSplat架构：FPN网络为每张输入图像I_i预测2D特征图。使用基于单应性的特征变形（warping），通过相对位姿P_i^{-1}P_v将特征图扭曲到目标视图。随后两个并行分支：(1)DeepMVS分支——遵循经典多视图立体匹配架构，将特征融合到代价体中，经3D卷积网络处理为概率体，得到深度预测D；(2)像素对齐预测分支——将特征池化到目标特征图F_v，经2D卷积网络和MLP映射到高斯基元属性（缩放、旋转、不透明度、颜色等）。DeepMVS深度通过相机内参反投影得到面片的3D位置属性mu：mu = unproject(D, K^{-1})，由此形成最终的完整属性图Phi_v = {mu, s, q, alpha, c}。\n\n渲染采用2DGS的混合渲染方式。测试时重建使用TSDF深度融合：对每个视图v，使用由推断面片图Phi_v构成的高斯基元集合进行深度溅射，然后通过TSDF算法融合所有深度获得显式三角网格。\n\n**基础模型特征集成**\n\n为进一步提升特征质量，集成了两类深度视觉特征：\n\nMASt3R多视图特征：MASt3R是一个在大规模数据上训练的SOTA 3D基础模型，学习在特征空间中建立图像对之间的对应关系。输入两张图像，输出稠密点图和立体特征图。对所有输入图像对运行MASt3R，每张图像的特征定义为该图像与所有其他视图组合获得的特征图的拼接：F_i^{MASt3R} = [F_{i,1}, ..., F_{i,N}]。该特征直接作为FPN网络的额外输入，丰富了代价体的特征表示，使得预测深度和后续网格重建更加准确。\n\nDINOv2单目特征：提取图像特征图并上采样到输入分辨率：F_i^{DINO} = upsample(DINOv2(I_i))。最终FPN网络的输入为图像与其单目特征的拼接。\n\n**多阶段训练与损失函数**\n\n模型端到端训练，使用多视图RGB图像和真值深度作为监督，深度特征网络（MASt3R、DINOv2）保持冻结以节省计算。采用粗到细的多阶段训练过程：目标图像从邻近输入视图可微重建。\n\n每阶段的损失函数包含：L_mse（渲染图像与真值之间的MSE损失）、L_ssim（结构相似性损失）、L_perc（感知损失，基于VGG特征）、L_depth（预测深度与真值深度之间的L1损失）、L_dist（深度失真损失，最初在Mip-NeRF 360中引入，用于集中射线上权重分布，惩罚权重分散在多峰上）以及L_normal（法向一致性损失，确保2D面片局部与实际表面几何对齐）。\n\n深度失真损失L_dist = Sigma_{i,j} w_i w_j |z_i - z_j|，其中w_i、w_j为混合权重，z_i、z_j为沿观察方向的深度值。法向一致性损失L_normal = Sigma_i w_i (1 - n_i^T n_hat)，其中n_i为面片法向，n_hat为从渲染深度图推导的法向。\n\n由于MVS架构采用金字塔设计，L_s表示第s阶段的损失加权组合。总损失为各阶段损失的加权和。值得注意的是，与无法在训练时渲染全图和全深度（受计算时间限制）的隐式方法不同，基于高效高斯溅射渲染的本方法能够在训练时将损失施加于整张图像。"
  },
  "2505_22421v2": {
    "technical": "GeoDrive提出了一种混合神经-几何框架，将显式3D几何条件集成到驾驶世界模型中，以增强空间理解和动作可控性。给定初始参考图像I_0和自车轨迹T={T_1,...,T_N}，框架合成遵循输入轨迹的真实感未来视频帧。整体流程包括三个阶段：3D表示重建、沿轨迹渲染与动态编辑、以及潜在视频扩散生成。\n\n**3D场景重建**\n\n从单张输入图像I_0构建3D表示。采用MonST3R，一个现成的稠密立体模型，同时预测3D几何和相机位姿。推理时将参考图像复制以满足MonST3R的跨视图匹配要求。给定RGB帧I_0和I_0'，MonST3R通过跨视图特征匹配预测每像素3D坐标X和置信度。通过阈值化置信度（通常tau=0.5），得到参考帧的彩色点云：P_0 = {X_p, c_p | conf_p > tau}，其中X_p为参考帧中像素p的度量空间位置，c_p为其颜色。\n\n为解耦静态场景几何和移动物体，MonST3R使用基于Transformer的解耦器。该模块处理参考帧的初始特征（经跨视图上下文增强），分离为静态和动态分量。解耦器使用可学习提示token分离注意力图：静态token关注大面积平面区域，动态token关注紧凑的运动丰富区域。排除动态对应关系后，获得鲁棒的相机位姿估计：T = argmin_T Sigma_p conf_p . rho(pi(T . X_p) - u_p)，仅使用静态特征匹配。相比传统SfM，该策略在动态城市场景中减少38%的位姿误差。所得点云作为后续几何支架。\n\n**沿轨迹渲染与动态编辑**\n\n将参考点云通过用户提供的每个相机配置进行投影，渲染出作为生成过程视觉引导的视频序列。每个3D点X经刚体变换到相机坐标系：X_cam = T_i . X，再通过相机内参K_i进行透视投影：u = pi(K_i . X_cam)。仅考虑深度范围在0~200米内的有效投影，使用z-buffering处理遮挡，最终产生每个相机位置的渲染视图R_i。\n\n然而仅使用第一帧点云会导致渲染场景全程保持静态，与真实驾驶场景中车辆和其他动态物体持续运动的情况存在显著差异。为此提出动态编辑（Dynamic Editing，如图[Figure N]所示）：当用户提供场景中移动车辆的2D边界框序列信息时，动态调整其位置以在渲染中创建运动效果。该设计不仅引导生成过程中自车轨迹的跟随，还指导场景中其他车辆的运动，显著缩小了静态渲染与动态真实场景之间的差距，同时实现了对其他车辆的灵活控制——这是Vista和GAIA等现有方法无法实现的能力。\n\n**潜在视频扩散生成**\n\n虽然基于点云的渲染能精确保持视图间的几何关系，但存在遮挡、传感器覆盖范围限制导致的缺失区域以及相比真实相机图像更低的视觉保真度等问题。为此，采用潜在视频扩散模型来细化投影视图，同时通过专门的条件机制保持3D结构保真度。\n\n借鉴VideoPainter的方法，将上下文特征集成到预训练的扩散Transformer（DiT）中，但做了针对性的改进。使用动态渲染来捕获时间和上下文细微差别。设h_l为DiT主干第l层的特征输出，z_dyn为动态渲染经VAE编码器的潜在表示，z_t为时间步t的噪声潜在。渲染通过轻量条件编码器处理，提取必要的背景线索（不复制主干的大量参数）。特征融合公式为：h_tilde_l = h_l + phi_l(zero_init) . E_cond(z_t, z_dyn)^{(l)}，其中E_cond为处理噪声潜在和渲染潜在拼接输入的条件编码器，L为DiT主干总层数，phi_l为初始化为零的可学习线性变换，以防止训练早期噪声崩溃。\n\n提取的特征以结构化方式选择性融合到冻结的DiT中，确保仅相关上下文信息引导生成过程。最终视频序列通过冻结的VAE解码器解码。通过仅训练条件编码器（占总参数量的6%），保持了预训练模型的真实感并获得了精确的相机控制。时序连贯性自然来源于视频Transformer的动力学建模以及跨帧特征之间的几何一致性，实现了轨迹忠实的高质量视频合成。"
  }
}

# Write properly escaped JSON
with open('C:/Users/XPENG_USER/Documents/docs/research/feedforward_recovery/docs/tmp_methods/output_technical_all.json', 'w', encoding='utf-8') as f:
    json.dump(output, f, ensure_ascii=False, indent=2)

# Verify
with open('C:/Users/XPENG_USER/Documents/docs/research/feedforward_recovery/docs/tmp_methods/output_technical_all.json', 'r', encoding='utf-8') as f:
    verified = json.load(f)

print(f'Total keys: {len(verified)}')
for k, v in verified.items():
    print(f'{k}: {len(v["technical"])} chars')
